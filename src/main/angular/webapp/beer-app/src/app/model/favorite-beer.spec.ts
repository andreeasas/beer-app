import { FavoriteBeerResponse } from './favorite-beer-response';

describe('FavoriteBeer', () => {
  it('should create an instance', () => {
    expect(new FavoriteBeerResponse()).toBeTruthy();
  });
});
